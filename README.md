# TutorLab

Веб-приложение для помощи репетиторам в организации учебного процесса с поддержкой живых онлайн-уроков.

## 🚀 Основные возможности

- **Управление учениками**: создание, просмотр, удаление учеников, добавление материалов и дат занятий
- **Живые онлайн-уроки**: 
  - Загрузка PDF презентаций с автоматической конвертацией в слайды
  - Рисование на слайдах в реальном времени
  - Указатель преподавателя для навигации по слайдам
  - WebRTC аудио-связь между преподавателем и учениками
  - Синхронизация слайдов и действий через WebSocket
- **Профиль репетитора**: регистрация, вход, настройки профиля
- **Календарь занятий**: визуализация и управление расписанием
- **Загрузка файлов**: материалы для учеников и фотографии

## 🛠 Технологический стек

### Backend
- **Java 21** с **Spring Boot 3.5.8**
- **Redis** - хранение данных (TTL 30 дней)
- **Spring WebSocket** (STOMP) - для синхронизации в реальном времени
- **Apache PDFBox** - конвертация PDF в изображения
- **Maven** - управление зависимостями

### Frontend
- **React 18** с **Vite**
- **React Router** - маршрутизация
- **WebSocket (STOMP)** - синхронизация действий
- **WebRTC (simple-peer)** - аудио-связь
- **Axios** - HTTP клиент

## 📁 Структура проекта

```
TutorLab/
├── src/main/java/project/TutorLab/
│   ├── config/              # Конфигурации (Redis, WebSocket, CORS)
│   ├── controller/          # REST контроллеры
│   │   ├── TutorController.java
│   │   ├── StudentController.java
│   │   ├── LiveSessionController.java
│   │   ├── LiveSessionWsController.java
│   │   └── FileUploadController.java
│   ├── dto/                 # Data Transfer Objects
│   │   └── live/            # DTO для живых сессий
│   ├── model/               # Модели данных
│   │   ├── Tutor.java
│   │   ├── Student.java
│   │   └── live/LiveSessionState.java
│   ├── repository/          # Репозитории для работы с Redis
│   └── service/             # Бизнес-логика
│       └── impl/            # Реализации сервисов
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── registration/  # Регистрация репетитора
│   │   │   ├── login/         # Вход в систему
│   │   │   ├── home/          # Главная страница со списком учеников
│   │   │   ├── student/       # Страница ученика
│   │   │   ├── live/          # Компоненты живых уроков
│   │   │   └── settings/      # Настройки профиля
│   │   ├── services/
│   │   │   ├── api.js         # HTTP API клиент
│   │   │   ├── wsClient.js    # WebSocket клиент
│   │   │   └── webrtcService.js # WebRTC сервис
│   │   ├── App.jsx
│   │   └── main.jsx
│   └── package.json
├── docker-compose.yml       # Конфигурация Redis
└── pom.xml                  # Maven конфигурация
```

## 📋 Требования

- **Java 21**
- **Maven 3.6+**
- **Node.js** (версия указана в `frontend/.nvmrc`)
- **npm** или **yarn**
- **Docker** и **Docker Compose** (для запуска Redis)

## 🚀 Запуск проекта

### 1. Запуск Redis через Docker Compose

```bash
docker-compose up -d
```

Redis будет доступен на `localhost:6379` и автоматически перезапускаться при перезагрузке системы.

**Проверка работы Redis:**
```bash
# Проверить статус контейнера
docker-compose ps

# Проверить логи
docker-compose logs redis

# Остановить Redis
docker-compose down

# Остановить с удалением данных
docker-compose down -v
```

**Альтернативно:** Если Redis установлен локально:
```bash
redis-server
```

### 2. Запуск Backend

```bash
# Из корневой директории проекта
./mvnw spring-boot:run
```

Backend будет доступен на `http://localhost:8080`

### 3. Запуск Frontend

```bash
cd frontend
npm install
npm run dev
```

Frontend будет доступен на `http://localhost:5173` (или другом порту, указанном Vite)

**Production сборка:**
```bash
npm run build
npm run preview
```

## 📡 API Endpoints

### Репетиторы

- `POST /api/tutors/register` - Регистрация нового репетитора
- `POST /api/tutors/login` - Вход в систему
- `GET /api/tutors/{id}` - Получить информацию о репетиторе
- `PUT /api/tutors/{id}` - Обновить информацию о репетиторе
- `GET /api/tutors/{id}/exists` - Проверить существование репетитора
- `GET /api/tutors/login/{login}/exists` - Проверить существование логина

### Ученики

- `POST /api/students/tutor/{tutorId}` - Создать нового ученика
- `GET /api/students/{id}` - Получить информацию об ученике
- `GET /api/students/tutor/{tutorId}` - Получить всех учеников репетитора
- `POST /api/students/{id}/materials` - Добавить материал к ученику
- `POST /api/students/{id}/lessons` - Добавить дату занятия
- `DELETE /api/students/{id}` - Удалить ученика
- `POST /api/students/{id}/toggle-favorite` - Добавить/убрать из избранного

### Живые уроки

- `POST /api/live/sessions` - Создать новую сессию урока
- `GET /api/live/sessions/{sessionId}` - Получить информацию о сессии
- `POST /api/live/sessions/{sessionId}/presentation` - Загрузить PDF презентацию
- `GET /api/live/sessions/{sessionId}/presentation` - Получить презентацию
- `PUT /api/live/sessions/{sessionId}/slide` - Изменить текущий слайд
- `GET /api/live/sessions/{sessionId}/slides/{slideIndex}/drawings` - Получить рисунки на слайде
- `GET /api/live/slides/{sessionId}/{filename}` - Получить изображение слайда
- `POST /api/live/sessions/{sessionId}/clear-drawings` - Очистить рисунки на слайде
- `DELETE /api/live/sessions/{sessionId}` - Удалить сессию

### Загрузка файлов

- `POST /api/upload` - Загрузить файл (фото или материал)

### WebSocket

- Подключение: `/ws/live/{sessionId}`
- Топики:
  - `/topic/session/{sessionId}/slide` - изменение слайда
  - `/topic/session/{sessionId}/draw` - рисование
  - `/topic/session/{sessionId}/pointer` - указатель
  - `/topic/session/{sessionId}/clear` - очистка
  - `/topic/session/{sessionId}/presentation` - обновление презентации
  - `/topic/session/{sessionId}/webrtc` - WebRTC сигналы

## 🎯 Функциональность

### Регистрация и вход
- Регистрация репетитора с логином и паролем
- Вход в систему
- ID репетитора сохраняется в localStorage браузера
- Данные хранятся в Redis

### Главная страница
- Отображение карточек всех учеников репетитора
- Каждая карточка содержит: имя, фамилию, возраст, фото
- Возможность добавления нового ученика
- Переход к странице ученика по клику на карточку
- Избранные ученики отображаются первыми

### Страница ученика
- Полная информация об ученике
- Список интересов
- Календарь с датами занятий
- Список прикрепленных материалов
- Возможность добавления новых материалов и дат занятий
- Удаление ученика

### Живые уроки

#### Для преподавателя:
- Создание новой сессии урока
- Загрузка PDF презентации (автоматическая конвертация в слайды)
- Навигация по слайдам (вперед/назад)
- Рисование на слайдах (ручка с настройкой цвета и толщины)
- Указатель для навигации
- Очистка рисунков на слайде
- Включение/выключение микрофона
- Отключение звука
- Копирование ссылки для учеников
- Сохранение рисунков для каждого слайда

#### Для ученика:
- Подключение к уроку по ссылке
- Просмотр слайдов в реальном времени
- Видимость рисунков преподавателя
- Видимость указателя преподавателя
- Прослушивание аудио через WebRTC
- Автоматическая синхронизация при смене слайдов

### Настройки профиля
- Просмотр и редактирование информации о репетиторе
- Загрузка фотографии профиля

## 📝 Примечания

- Все данные хранятся в Redis с TTL 30 дней
- ID репетитора используется для идентификации во всех запросах
- Frontend использует React Router для навигации
- PDF файлы конвертируются в PNG изображения (по одному на страницу)
- WebRTC использует STUN серверы Google для установления соединения
- Рисунки сохраняются отдельно для каждого слайда
- Максимальный размер загружаемого файла: 10MB

## 🔧 Конфигурация

### Backend (`application.properties`)
```properties
spring.data.redis.host=localhost
spring.data.redis.port=6379
server.port=8080
spring.servlet.multipart.max-file-size=10MB
app.upload.dir=users-photos
app.upload.materials.dir=materials
```

### Frontend
- Порт по умолчанию: 5173 (Vite)
- API endpoint: `http://localhost:8080/api`
- WebSocket endpoint: `ws://localhost:8080/ws`

## 📄 Лицензия

Проект создан в учебных целях.
